/** @type {import('tailwindcss').Config} */
// Tailwind v4 is CSS-first (config lives in globals.css via @theme /
// @custom-variant). This file is kept only because components.json still
// points "tailwind.config" at it — left intentionally empty.
module.exports = {};
